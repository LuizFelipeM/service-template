name: ${PROJECT_NAME}

services:
  service:
    container_name: ${PROJECT_NAME}
    image: ${PROJECT_NAME}
    environment:
      NODE_ENV: production
      # HTTP Server port
      PORT: 3000
    ports:
      # HTTP Server port
      - 3000:3000
    expose:
      - 3000
    command: node
